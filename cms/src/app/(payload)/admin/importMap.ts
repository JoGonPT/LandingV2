/**
 * Mapa de importações do painel.
 *
 * Gerado por `npx payload generate:importmap`. Fica vazio enquanto não houver
 * componentes personalizados no admin — que é o caso nesta fase.
 */
export const importMap = {};
