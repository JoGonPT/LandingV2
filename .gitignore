.next
out
node_modules
.data
*.log
.vercel
.DS_Store
tsconfig.tsbuildinfo

# Segredos — nunca versionar.
# `.env.production` é criado pelo audit_env.sh (vercel env pull) com todos os
# segredos de produção em texto simples; sem esta regra ia num `git add .`.
.env
.env.*
!.env.example
coverage

# CMS (Payload) — projeto npm independente, com o seu próprio Next
cms/node_modules/
cms/.next/
cms/.env.local
cms/.env*.local
